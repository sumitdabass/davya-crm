# SP#1 Dynamic Pipelines & Stages — Resume Handoff

**Paused:** 2026-04-23 evening
**Resume trigger:** "resume davyas crm pending task"
**Branch:** `feature/dynamic-pipelines-stages` (not yet pushed, 28 commits ahead of `main`)
**Test suite at pause:** ~467 tests green, 0 failures (ignore the PHP 8.5 `PDO::MYSQL_ATTR_SSL_CA` deprecations — harmless)

## The 30-second context

Building SP#1 of the 3-phase "zero-code admin configuration" roadmap. Replaces the hardcoded `App\Enums\PipelineStage` enum + `App\Services\StageTransitionValidator` with DB-backed, admin-editable pipelines/stages/rules behind a new `/admin/pipeline-config` Filament page. Existing `/admin/kanban` keeps its URL; all 4 hardcoded validator rules are preserved byte-for-byte as seeded DB rows.

**Authoritative docs:**
- Spec: `docs/superpowers/specs/2026-04-23-dynamic-pipelines-stages-design.md`
- Plan: `docs/superpowers/plans/2026-04-23-dynamic-pipelines-stages.md` (22 tasks, bite-sized TDD)

## Progress — 17 of 22 tasks shipped

| # | Task | Status | Commit |
|---|---|---|---|
| 1 | pipelines + stages tables | ✅ | `b62c8d8` |
| 2 | transition rules + conditions tables | ✅ | `7b443ce` |
| 3 | students.stage_id FK + widen stage col | ✅ | `00b2ca8` |
| 4 | Pipeline + Stage models | ✅ | `fc3eb62` |
| 5 | StageTransitionRule + Condition models | ✅ | `6f17b8d` |
| 6 | seed IPU Admission pipeline + 13 stages | ✅ | `97cd44b` + `0f6b49c` fix |
| 7 | seed 4 default transition rules | ✅ | `6836b75` |
| 8 | backfill students.stage_id | ✅ | `1e82bbd` + `8f3a401` fix |
| 9 | PipelineConfig cached reader | ✅ | `f6b85ac` |
| 10 | StageRepository create/rename/reorder | ✅ | `c789c43` + `5d48d33` fix |
| 11 | StageRepository delete-with-transfer + changeType | ✅ | `65e0fa2` + `6c2404e` fix |
| 12 | ConditionEvaluator | ✅ | `7f664cc` + `62634e5` fix |
| 13 | StageTransitionEngine | ✅ | `a2fe6a3` + `1b1e9fa` fix |
| 14 | swap callers from legacy enum/validator | ✅ | `39be354` + `7d6a72f` + `f0f76f8` fixes |
| 15 | PipelineConfigPage scaffold + admin gate | ✅ | `5f51dff` + `cf4fbb6` fix |
| 16 | stages-tab create/rename/delete actions | ✅ | `39f71e4` |
| 17 | stages-tab drag-reorder | ✅ | `702445d` |
| 18 | Rules tab — list existing rules | ⏳ pending | — |
| 19 | Rules tab — create/edit/toggle | ⏳ pending | — |
| 20 | delete dead code (enum + validator) | ⏳ pending | — |
| 21 | end-to-end smoke test | ⏳ pending | — |
| 22 | deploy runbook | ⏳ pre-deploy STOP | — |

## Outstanding review follow-ups to address on resume

These are known gaps flagged by reviewers that were deferred:

### T16 follow-up (Filament stages-tab — 2 items)

1. **Alpine modal closes on server error (user loses draft).**
   File: `resources/views/filament/pages/pipeline-config.blade.php`
   In the "New Stage" modal's Create button, change:
   ```html
   @click="$wire.createStage(name, type); open = false"
   ```
   to:
   ```html
   @click="$wire.createStage(name, type).then(() => open = false)"
   ```

2. **`renameStage` and `changeStageType` are live Livewire endpoints with no UI wiring and no tests.**
   T18 / T19 should either wire them (preferred — they're natural actions for the ⋯ menu + Won/Lost type toggle) or delete them.

### T2 follow-up (deferred to T19)

3. **"Both sides NULL" rule CHECK is app-layer only.**
   Spec line 90 says a rule with `from_stage_id IS NULL AND to_stage_id IS NULL` is meaningless and must be rejected. T19's `saveRule` method must validate this explicitly:
   ```php
   if (empty($data['from_stage_id']) && empty($data['to_stage_id'])) {
       Notification::make()->title('Rule must specify at least one side (from or to stage).')->danger()->send();
       return;
   }
   ```

### Minor polish (non-blocking)

- Stage name modal needs `minlength="1" maxlength="60" required` on the input + `:disabled="!name.trim()"` on Create button.
- T16 delete test could also assert the migrated student's `stage_id` equals the target.
- T20 cleanup note: the inline comments in `PipelineSummary.php` that say "Mirrors PipelineStage::X->value" become meaningless once the enum is deleted — drop or rephrase.

## What's still to build (T18 → T22)

### T18 — Rules tab: list existing rules
Plan section: "Phase 6 — Filament admin UI → Task 18" in `docs/superpowers/plans/2026-04-23-dynamic-pipelines-stages.md`.
- Add `getRules()` helper method on `PipelineConfigPage` returning the default pipeline's rules with eager-loaded `fromStage`, `toStage`, `conditions`.
- Replace the Blade `@else` branch (currently "Rules tab — populated in Task 18.") with the rules-card render from the plan.
- Assert `test_rules_tab_lists_4_seeded_rules` — switches tab + sees all 4 seeded rule names.

### T19 — Rules tab: create/edit/toggle/delete
- Page methods: `saveRule(array $data, ?int $ruleId = null)`, `toggleRule(int $ruleId)`, `deleteRule(int $ruleId)`.
- **Must add the "both sides NULL" app-level guard** (follow-up #3 above).
- Alpine rule-editor modal with from/to stage selects, Hard/Soft radio, condition repeater.
- Tests: `test_admin_can_create_rule`, `test_admin_can_toggle_rule_active`.

### T20 — Delete dead code
- `grep -rn "App\\\\Enums\\\\PipelineStage\|StageTransitionValidator" app/ tests/` — should return only the two files being deleted plus possibly legacy test references that need swap-outs.
- `ActivityDescriber` was already rewired in T14 follow-up (commit `f0f76f8`) — moved `PipelineStage::fromRoundName` into `App\Support\RoundNameToStage` — so T20 should be clean.
- Delete `app/Enums/PipelineStage.php` and `app/Services/StageTransitionValidator.php`.
- Full test suite must stay green.

### T21 — End-to-end smoke test
- `tests/Feature/Pipeline/PipelineEndToEndTest.php` — walks a student from Lead Captured through to Closed and back, asserting hard/soft rule behavior at each transition.

### T22 — Deploy runbook (🛑 PRE-DEPLOY PAUSE POINT)
- Write `docs/sessions/2026-04-23-dynamic-pipelines-stages-runbook.md`.
- **Stop before `git push` / deploy** — wait for Sumit's go-ahead.

## How to resume

Say "resume davyas crm pending task" (or equivalent). The agent should:

1. Read this file (`docs/superpowers/sessions/2026-04-23-sp1-resume.md`).
2. Checkout/confirm `feature/dynamic-pipelines-stages` branch.
3. Confirm working tree clean, test suite still green.
4. Address the T16 follow-up Alpine modal fix inline (one-line change) — quick win to clear the deck.
5. Resume subagent-driven execution at T18 using the plan's Task 18 block verbatim.
6. Continue T18 → T19 → T20 → T21 → **PAUSE at T22** for user review.

## Relevant paths (quick reference)

- `app/Services/Pipeline/` — PipelineConfig, StageRepository, ConditionEvaluator, StageTransitionEngine
- `app/Filament/Pages/PipelineConfigPage.php` — the admin settings page
- `resources/views/filament/pages/pipeline-config.blade.php` — its view
- `app/Filament/Pages/KanbanBoard.php` — existing kanban, rewired in T14 to use dynamic stages
- `app/Support/RoundNameToStage.php` — new helper that replaced `PipelineStage::fromRoundName`
- `tests/Feature/Pipeline/` — 8 Pipeline test files, all green
- Still-alive-but-to-be-deleted in T20: `app/Enums/PipelineStage.php`, `app/Services/StageTransitionValidator.php`
